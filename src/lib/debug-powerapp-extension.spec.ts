import { debugPowerappExtension } from './debug-powerapp-extension';

describe('debugPowerappExtension', () => {
  it('should work', () => {
    expect(debugPowerappExtension()).toEqual('debug-powerapp-extension');
  });
});
